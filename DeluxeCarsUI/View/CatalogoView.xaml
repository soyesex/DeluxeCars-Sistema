<UserControl x:Class="DeluxeCarsUI.View.CatalogoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeluxeCarsUI.View"
             mc:Ignorable="d" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             Background="Transparent"
             FontFamily="Montserrat">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PlaceholderBrush" Color="#b0b9c6" />
        <SolidColorBrush x:Key="color1" Color="#512bd4" />
        <SolidColorBrush x:Key="color3" Color="#fff" />

        <Style x:Key="NoHoverColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#343a40"/>
        </Style>

        <Style x:Key="CenterContentStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="130"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontFamily" Value="Montserrat"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="{StaticResource color3}"/>
            <Setter Property="Background" Value="{StaticResource color1}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="15" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#28AEED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Catálogo de Productos"
                   Foreground="White"
                   FontSize="25"
                   FontWeight="Normal"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical">

                <TextBox Style="{StaticResource SearchBoxStyle}" 
                         Tag="Buscar producto..." />

                <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                    <Button Content="Filtrar prod" Style="{StaticResource DarkActionButtonStyle}"  Margin="0,0,10,0"/>
                    <Button Content="Nuevo prod" Style="{StaticResource DarkActionButtonStyle}"  Margin="0,0,10,0"/>
                    <Button Content="Categorías" Style="{StaticResource DarkActionButtonStyle}" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Top">

                <ComboBox x:Name="cbxFiltroCategoria" Width="200" Height="40"
                      Style="{StaticResource DarkComboBoxStyle}" 
                      MinWidth="200" 
                      SelectedIndex="0">

                    <ComboBoxItem Content="Categoría" />
                    <ComboBoxItem Content="Motor" />
                    <ComboBoxItem Content="Suspensión" />
                    <ComboBoxItem Content="Frenos" />
                    <ComboBoxItem Content="Accesorios" />
                </ComboBox>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" CornerRadius="10" Background="{StaticResource PrimaryBackgroundColor}" BorderBrush="{StaticResource PrimaryBorderColor}" BorderThickness="1">
            <DataGrid x:Name="ProductsDataGrid"
              Style="{StaticResource ElegantDataGridStyle}"
              ItemsSource="{Binding Productos}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Imagen" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="5" Width="40" Height="40" Margin="5">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                    </Border.Background>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>