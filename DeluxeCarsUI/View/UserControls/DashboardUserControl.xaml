<UserControl x:Class="DeluxeCarsUI.View.UserControls.DashboardUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeluxeCarsUI.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             Background="Transparent"
             FontFamily="Montserrat"
             Foreground="#E0E6F1">

    <UserControl.Resources>
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1F2A44"/>
            <Setter Property="BorderBrush" Value="#4D5B7C"/>
            <Setter Property="Foreground" Value="#E0E6F1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#28AEED"/>
                                <Setter TargetName="border" Property="Background" Value="#2C3A5A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25324E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Dashboard Pro"
                   FontSize="28"
                   FontWeight="Bold"
                   HorizontalAlignment="Left"
                   Margin="10,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Style="{StaticResource CardButtonStyle}" Margin="10">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Ventas Totales"/>
                        <fa:IconImage Icon="Coins" Opacity="0.8"/>
                    </StackPanel>
                </Button>

                <Button Grid.Row="1" Style="{StaticResource CardButtonStyle}" Margin="10">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Productos Populares"/>
                        <fa:IconImage Icon="ChartSimple" Opacity="0.8"/>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Style="{StaticResource CardButtonStyle}" Margin="10">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Ventas del Mes"/>
                        <fa:IconImage Icon="ChartColumn"  Opacity="0.8"/>
                    </StackPanel>
                </Button>

                <Button Grid.Row="1" Style="{StaticResource CardButtonStyle}" Margin="10">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Estadísticas Clave"/>
                        <fa:IconImage Icon="Key" Opacity="0.8"/>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Column="2">
                <Button Style="{StaticResource CardButtonStyle}" Margin="10">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitleStyle}" Text="Movimientos Inventario"/>
                        <fa:IconImage Icon="BoxOpen"  Opacity="0.8"/>
                    </StackPanel>
                </Button>
            </Grid>

        </Grid>
    </Grid>
</UserControl>