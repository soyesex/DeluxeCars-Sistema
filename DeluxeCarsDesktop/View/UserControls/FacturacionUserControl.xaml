<UserControl x:Class="DeluxeCarsDesktop.View.UserControls.FacturacionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeluxeCarsDesktop.View.UserControls"
             mc:Ignorable="d" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             Background="Transparent"
             FontFamily="Montserrat"
             Foreground="#E0E6F1">

    <UserControl.Resources>
        <Style x:Key="DarkInputControlStyle" TargetType="Control">
            <Setter Property="Background" Value="#1F2A44" />
            <Setter Property="Foreground" Value="#E0E6F1" />
            <Setter Property="BorderBrush" Value="#4D5B7C" />
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#28AEED"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#28AEED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource DarkInputControlStyle}"/>
        <Style x:Key="DarkDatePickerStyle" TargetType="DatePicker" BasedOn="{StaticResource DarkInputControlStyle}"/>

        <Style x:Key="DarkActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1F2A44"/>
            <Setter Property="Foreground" Value="#E0E6F1"/>
            <Setter Property="BorderBrush" Value="#4D5B7C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="15" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2C3A5A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#28AEED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Facturación" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,25"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,10,0">
                <TextBox Style="{StaticResource SearchBoxStyle}" 
                         Tag="Buscar factura..." />
                <Button Content="Servicios"
                        Command="{Binding ShowServicioFormCommand}" Style="{StaticResource DarkActionButtonStyle}" Margin="0,10,0,0"/>
                <Button Content="Métodos de pago"
                        Command="{Binding ShowMetodoPagoFormCommand}" Style="{StaticResource DarkActionButtonStyle}" Margin="0,10,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0">
                <Button Content="Nueva factura"
                        Command="{Binding ShowFacturaFormCommand}" Style="{StaticResource DarkActionButtonStyle}" Margin="0 0 10 0"/>
                <Button Content="Ver detalles fact"
                        Command="{Binding ShowDetallesFacturaFormCommand}" Style="{StaticResource DarkActionButtonStyle}"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Vertical" Margin="10,0,0,0">
                <ComboBox x:Name="cbxCliente" Style="{StaticResource DarkComboBoxStyle}" SelectedIndex="0">
                    <ComboBoxItem Content="Cliente"/>
                </ComboBox>
                <DatePicker x:Name="datePickerMes" Style="{StaticResource DarkDatePickerStyle}" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" CornerRadius="10" Background="{StaticResource PrimaryBackgroundColor}" 
                BorderBrush="{StaticResource PrimaryBorderColor}" BorderThickness="1">
            <DataGrid x:Name="ProductsDataGrid"
                      Style="{StaticResource ElegantDataGridStyle}"
                      ItemsSource="{Binding Productos}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Imagen" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="5" Width="40" Height="40" Margin="5">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                    </Border.Background>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="3" Text="Total facturado: $" FontSize="14" HorizontalAlignment="Left" Margin="10,15,0,0"/>
    </Grid>
</UserControl>