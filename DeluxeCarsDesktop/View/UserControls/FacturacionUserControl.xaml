<UserControl x:Class="DeluxeCarsDesktop.View.UserControls.FacturacionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeluxeCarsDesktop.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#4D5B7C" BorderThickness="0,0,0,1" Padding="5" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                </Grid.ColumnDefinitions>
                <Label Content="Buscar Cliente:" VerticalAlignment="Center" Style="{StaticResource DarkLableForm}"/>
                <ComboBox Grid.Column="1" x:Name="ClientSearchComboBox" Style="{StaticResource DarkComboBoxStyle}"
                          ItemsSource="{Binding ResultadosBusquedaCliente}"
                          DisplayMemberPath="Nombre"
                          IsEditable="True"
                          Text="{Binding TextoBusquedaCliente, UpdateSourceTrigger=PropertyChanged}"/>

                <Label Grid.Column="2" Content="Método de Pago:" VerticalAlignment="Center" Style="{StaticResource DarkLableForm}" Margin="10,0,0,0"/>
                <ComboBox Grid.Column="3" ItemsSource="{Binding MetodosDePago}"
                          SelectedItem="{Binding MetodoPagoSeleccionado}"
                          DisplayMemberPath="Descripcion" Style="{StaticResource DarkComboBoxStyle}"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="5">
        </Grid>

        <Border Grid.Row="2" CornerRadius="10" Background="{StaticResource PrimaryBackgroundColor}" BorderBrush="{StaticResource PrimaryBorderColor}" BorderThickness="1">
            <DataGrid ItemsSource="{Binding LineasDeFactura}" Style="{StaticResource ElegantDataGridStyle}" CanUserAddRows="False" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                    <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="Auto"/>
                    <DataGridTextColumn Header="Precio Unit." Binding="{Binding PrecioUnitario, StringFormat=C}" Width="Auto"/>
                    <DataGridTextColumn Header="Subtotal" Binding="{Binding SubTotalLinea, StringFormat=C}" Width="Auto"/>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Quitar" Command="{Binding DataContext.EliminarItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Subtotal: " Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding SubTotal, StringFormat=C}" Foreground="White" FontSize="16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Text="IVA: " Foreground="White" FontSize="16" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <TextBlock Text="{Binding TotalIVA, StringFormat=C}" Foreground="White" FontSize="16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Text="TOTAL: " Foreground="White" FontSize="22" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <TextBlock Text="{Binding Total, StringFormat=C}" Foreground="LawnGreen" FontSize="22" VerticalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="FINALIZAR VENTA" Command="{Binding FinalizarVentaCommand}" Style="{StaticResource DarkActionButtonStyle}" Width="150" Background="ForestGreen"/>
                <Button Content="Cancelar" Command="{Binding CancelarVentaCommand}" Style="{StaticResource DarkActionButtonStyle}" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>