<UserControl x:Class="DeluxeCarsUI.View.UserControls.ConfiguracionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeluxeCarsUI.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             Background="Transparent"
             FontFamily="Montserrat"
             Foreground="#E0E6F1">

    <UserControl.Resources>
        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
        </Style>

        <Style x:Key="FormInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1F2A44" />
            <Setter Property="Foreground" Value="#E0E6F1" />
            <Setter Property="BorderBrush" Value="#4D5B7C" />
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#28AEED"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#28AEED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1F2A44"/>
            <Setter Property="Foreground" Value="#E0E6F1"/>
            <Setter Property="BorderBrush" Value="#4D5B7C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="180"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="15" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2C3A5A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#28AEED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nombre de la Tienda" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,25"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Dirección" Style="{StaticResource FormLabelStyle}"/>
                <TextBox   Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInputStyle}" Margin="0,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Teléfono" Style="{StaticResource FormLabelStyle}"/>
                <TextBox   Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInputStyle}" Margin="0,5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Email" Style="{StaticResource FormLabelStyle}"/>
                <TextBox   Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInputStyle}" Margin="0,5"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Horario de Atención" Style="{StaticResource FormLabelStyle}"/>
                <TextBox   Grid.Row="3" Grid.Column="1" Style="{StaticResource FormInputStyle}" Margin="0,5"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Descripción" Style="{StaticResource FormLabelStyle}"/>
                <TextBox   Grid.Row="4" Grid.Column="1" Style="{StaticResource FormInputStyle}" Margin="0,5"/>
            </Grid>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top">
                <Border Width="150" Height="150" CornerRadius="10" BorderBrush="#4D5B7C" BorderThickness="1" Background="#1F2A44" Margin="0,5,0,0">
                    <fa:IconImage Icon="Image" Foreground="#4D5B7C" Width="60" Height="60"/>
                </Border>
                <Button Content="Cambiar logo" Style="{StaticResource ActionButtonStyle}" Margin="0,15,0,0" Width="150"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" CornerRadius="15" Background="#1F2A44" BorderBrush="#4D5B7C" BorderThickness="1" Margin="0,30,0,0">
            <TextBlock Text="Banner de mi marca" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
        </Border>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Guardar Config" Style="{StaticResource ActionButtonStyle}" HorizontalAlignment="Left"/>
            <Button Grid.Column="1" Content="Cambiar banner" Style="{StaticResource ActionButtonStyle}" HorizontalAlignment="Right"/>
        </Grid>

    </Grid>
</UserControl>
